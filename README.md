## Scripts

```json

    "build": "next build",
    "dev": "next dev",  // start in dev mode
    "ui:add": "pnpm dlx shadcn@latest add", // add ui component from shadcn
    "lint": "next lint",
    "test": "exec playwright test",   // run test with playwright will work with localhost, you will need to run it too
    "start": "next start",
    "prepare": "husky"

```
